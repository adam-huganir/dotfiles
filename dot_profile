# Bash login shell (also read by sh login shells).
# Shared environment + helper functions live in ~/.shenv.
[ -s "$HOME/.shenv" ] && . "$HOME/.shenv"

# Login shells that are interactive should also get the interactive config.
case $- in
  *i*) [ -s "$HOME/.bashrc" ] && . "$HOME/.bashrc" ;;
esac
